# GATES — psychic-sidekick

| Gate | Scope | Evidence | Status |
|---|---|---|---|
| SDK-0 | Birth: zero-dependency contract-compiler UI, vendored 22-field vocabulary with sync check, department presets (defaults, not policy), node-if-present behavioral tests, validator with live negative controls, integration contract | Born under the parent psychic-crew HELIX gate SIDE-1; closure rides the same operator token, stamped in both ledgers | **APPROVED** `APPROVE SIDE-1` @ 2026-08-26T20:24:21Z |
| VIS-RECONCILE | 2026-08-31 | RETROACTIVE RECORD, append-only: this repo was flipped PUBLIC out-of-band on GitHub after birth — no token existed, no commit marks it, the flip date is unrecorded. Ratified public at S0-RECONCILE: the estate hazard audit (paths, identities, employer refs) returned CLEAN, enforced by this repo's own validator hygiene arms | recorded without a token; grandfathered from the explainer law by enumeration in docs/explainers/INDEX.md | recorded |
| S0-RECONCILE |  | MIT LICENSE + canonical identity (Nathan Lim) + explainer-epoch port + visibility ratified (VIS-RECONCILE) | multi-repo gate (one token, per-repo stamps after per-repo push verification); append-only history law held | **APPROVED** `APPROVE S0-RECONCILE` @ 2026-08-31T23:39:46Z |
| SIDE-R1 |  | the remote lane, brain first (no layout risk): REMOTE_PREAMBLE as a SINGLE-LINE literal (formatter-safe, round-trip proven) + compilePromptPack (wraps, never rewrites; unknown lane falls back to local) + docs/REMOTE-PROMPT-PROTOCOL.md with the fallback-rule INVERSION DECLARED OUT LOUD and the unasserted half stated + INTEGRATION-CONTRACT delta (boundary unchanged; 2 failure rows) | bound by D2 (two files, one byte-identity, vacuity-guarded, drift-fixture control) + E2r (defined+exported+tested; full ui wiring lands at SIDE-R2) + 6 node tests (E2r fired on its author: the first five never referenced the exported constant — the sixth binds pack === preamble+contract byte-for-byte) | revert unit: this gate's file set; pin: SIDE-R2 | **APPROVED** `APPROVE SIDE-R1` 2026-09-01T02:16Z (operator, exact token) |
| SIDE-R2 |  | the phone surface: a 2-lane Destination control (data-sw anchors) wires compilePromptPack into the page; ONE width query carries the mobile law (16px inputs, 44px tap targets, sticky compile bar, full-width controls); README From-your-phone + **2 lanes** bound every-occurrence; E2's used-in-ui loop now names the pack (compileContract keeps defined+exported+tested in E2r) | E3 static arms run on EVERY clone (width query, 16px/44px/sticky, lane count by two anchors, an overflow scanner fire-probed by a 900px div planted in a whole-repo scratch copy) + J2 renders at 390x844 where a browser exists: both lanes in DOM, wiring executed, an injected fit meter must report FITS and its planted 900px twin must report OVERFLOW — browserless hosts get a stated SKIP, the render legs are the operator machine's drill | revert unit: this gate's file set; pin: none (S5 rebinding follows) | **APPROVED** `APPROVE SIDE-R2` 2026-09-01T02:47Z (operator, exact token) |
| TPL-R1 |  | sidekick leg of the templates ladder gate: §F gains the extractor vacuity floor (>=20), the backticked-row fire-probe (the subset-blind hole: 3 backticked rows must shrink extraction below the floor), and the TEMPLATE-SET arm (UI keys subset of sibling stems; extras announced legal — the sibling now defines 6, the UI stays a curated 4); README phrase updated to say so | suite 60->63/0/0 with the sibling present; the absent leg stays the stated SKIP; closes second on this token, after templates | **APPROVED** `APPROVE TPL-R1` 2026-09-01T05:06Z (operator, exact token; sidekick leg 2 of 2 — templates closed ec57e18) |
